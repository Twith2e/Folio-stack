"ise client";

import { useState } from "react";

export default function WorkExperienceModal() {
  const [isOpen, setIsOpen] = useState(false);

  function openModal(e: HTMLDialogElement) {}

  return <dialog></dialog>;
}
